{{- if and (eq .Values.mode "combo") .Values.combo.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "clair.fullname" . }}
  labels:
    {{- include "clair.labels" . | nindent 4 }}
  {{- with .Values.combo.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.combo.ingress.className }}
  ingressClassName: {{ .Values.combo.ingress.className }}
  {{- end }}
  {{- with .Values.combo.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.combo.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "clair.fullname" $ }}
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
