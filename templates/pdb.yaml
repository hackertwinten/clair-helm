{{- if and (eq .Values.mode "combo") .Values.combo.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "clair.fullname" . }}
  labels:
    {{- include "clair.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.combo.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "clair.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: combo
{{- end }}
